3 module : UI Controler, Data Controller, Controller

- add event handler
- get input value
- add the new item to our structure
- add the new item to the UI
- Caculation budget
- update the UI






 tabNine
bracket pair colorizer 2
Auto rename tag

dùng splice() để delete 1 phần tử

dùng event.target để xác định click vào phần từ nào

removeChild xoá phần từ con